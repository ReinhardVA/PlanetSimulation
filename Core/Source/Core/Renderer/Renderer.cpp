#include "Renderer.h"
//void Renderer::Draw(const std::vector<Planet>& planets, const Planet& sun)
//{
//	sf::CircleShape sunShape(sun.GetRadius());
//	sunShape.setFillColor(sf::Color::Yellow);
//	sunShape.setOrigin({ sun.GetRadius(), sun.GetRadius() });
//	sunShape.setPosition({ m_windowSize.x/2.0f, m_windowSize.y/2.0f });
//	for(const auto& planet : planets){
//		sf::CircleShape shape(planet.GetRadius());
//		shape.setFillColor(sf::Color::Green);
//		shape.setPosition({ planet.GetX(), planet.GetY() });
//		m_window.draw(shape);
//	}
//	m_window.draw(sunShape);
//
//}


